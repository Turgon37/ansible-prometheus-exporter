---
# vars file for prometheus_exporter (Debian specific)

prometheus_exporter__required_packages_names:
  - libcap2-bin

prometheus_exporter__service_environment: '/etc/default/{{ prometheus_exporter__service_name }}'
